master_relationship_for_branch1:
  branch: branch
  user: user
  master: true
  admin: true
  
<% User.all[0..9].each_with_index do |user, i| %>
employee_relationship_<%= i %>:
  branch: branch
  user: <%= user %>
<% end %>