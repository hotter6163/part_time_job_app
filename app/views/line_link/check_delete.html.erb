<h2>LINEとのアカウント連携の解除</h2>
<p>本当に連携を解除する場合は、以下のボタンを押してください。</p>

<%= form_with(url: delete_link_path(@line_link), method: :delete, local: true) do |f| %>
  <%= hidden_field_tag :delete_token, params[:delete_token] %>
  
  <div class="text-center">
    <%= f.submit "連携を解除", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>